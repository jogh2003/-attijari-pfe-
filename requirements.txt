# ============================================================
# requirements.txt — PFE Attijari bank — Sujet 21
# pip install -r requirements.txt
# ============================================================

# ── Data & nettoyage ─────────────────────────────────────────
pandas==2.1.4
numpy==1.26.2
matplotlib==3.8.2
seaborn==0.13.0
openpyxl==3.1.2          # lecture fichiers Excel si besoin
xlrd==2.0.1               # lecture fichiers .xls Attijari bank (legacy Excel)

# ── Base de données ──────────────────────────────────────────
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0           # migrations BDD
redis==5.0.1
elasticsearch==8.11.1

# ── NLP ──────────────────────────────────────────────────────
spacy==3.7.2
sentence-transformers==2.2.2
transformers==4.36.2
torch==2.1.2              # requis par sentence-transformers

# ── Machine Learning & Deep Learning ─────────────────────────
tensorflow==2.14.0
scikit-learn==1.3.2
mlflow==2.9.2

# ── API Backend ──────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6   # upload de fichiers
websockets==12.0
httpx==0.25.2             # client HTTP async

# ── Sécurité ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7      # AES-256

# ── Variables d'environnement ────────────────────────────────
python-dotenv==1.0.0

# ── Tests ────────────────────────────────────────────────────
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1

# ── Utilitaires ──────────────────────────────────────────────
pydantic==2.5.2           # validation données FastAPI
pydantic-settings==2.1.0
python-dateutil==2.8.2
tqdm==4.66.1              # barres de progression
loguru==0.7.2             # logging avancé
schedule==1.2.1           # scheduler CRON Python (simple)
apscheduler==3.10.4       # scheduler avancé en arrière-plan (réentraînement LSTM)
